# 버전 관리의 개념

## 정의
버전 관리란 파일의 변경 이력을 체계적으로 기록하고,
원하는 시점의 상태로 되돌릴 수 있게 해주는 시스템이다.

## 버전 관리의 유형
- 로컬 버전 관리: 내 컴퓨터 안에서만 관리
- 중앙집중식(CVCS): 중앙 서버에서 이력 관리 (예: SVN)
- 분산형(DVCS): 각자 전체 이력 보유 (예: Git)

## Git이 DVCS인 이유
Git은 각 개발자의 로컬에 전체 이력이 복제되므로
서버 없이도 작업 가능하고, 장애 시에도 복구가 용이하다.